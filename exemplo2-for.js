const frutas = ["maça", "banana", "uva", "pera"];

// console.table(frutas.length) - realiza a contagem de itens dentro da lista

for (let i = 0; i < frutas.length; i++) {
    // console.log(frutas[i]);
    console.log(`${i} - ${frutas[i]}`);
}